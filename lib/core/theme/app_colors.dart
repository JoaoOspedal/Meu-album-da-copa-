import 'package:flutter/material.dart';

/// App-wide palette: white, blue and green, as requested for the album's
/// visual identity.
class AppColors {
  AppColors._();

  static const Color blue = Color(0xFF1565C0);
  static const Color blueLight = Color(0xFF42A5F5);
  static const Color green = Color(0xFF2E7D32);
  static const Color greenLight = Color(0xFF66BB6A);
  static const Color white = Color(0xFFFFFFFF);
  static const Color darkSurface = Color(0xFF0D1B2A);
  static const Color darkBackground = Color(0xFF071018);
  static const Color missingSticker = Color(0xFFBDBDBD);
}
